<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.simplepetmanager.model.mapper.BookmarkMapper">

    <!-- ✅ 유저 번호 기준으로 즐겨찾기 목록 조회 (팝업용) -->
    <select id="getBookmarkListByUser" resultType="com.project.simplepetmanager.model.dto.BookMark">
        SELECT user_number, Store_name, storeid
        FROM bookmark
        WHERE user_number = #{userNumber}
    </select>

    <!-- ✅ 특정 가게가 이미 즐겨찾기 되어 있는지 중복 체크 -->
    <select id="checkBookmarkExists" resultType="int">
        SELECT COUNT(*)
        FROM bookmark
        WHERE user_number = #{userNumber}
          AND storeid = #{storeId}
    </select>

    <!-- ✅ 컬럼-값 순서 수정 및 storeid 하드코딩 제거 -->
    <insert id="insertBookmarkList" >
        INSERT INTO bookmark (user_number, Store_name, Store_address)
        VALUES (#{userNumber}, #{storeName}, #{storeAddress})
    </insert>

    <!-- ✅ 즐겨찾기 삭제 (토글 해제용) -->
    <delete id="deleteBookmark">
        DELETE FROM bookmark
        WHERE user_number = #{userNumber}
          AND storeid = #{storeId}
    </delete>

</mapper>